{
    "name": "eliza",
    "private": true,
    "scripts": {
        "build": "pnpm --filter @elizaos/plugin-binance-scraper build && pnpm --filter @elizaos/core build && pnpm --filter @elizaos/agent build",
        "dev": "pnpm --parallel dev"
    },
    "devDependencies": {
        "turbo": "^2.3.3"
    }
}
